import std
import fileio
import args
import lex

function(int nargs, ArgList args) main -> int does
	if nargs!=2 do
		printf("E: Bad args\n")
		return 1
	done
	
	printf("Matching `%s` -> %i\n", args:get(1), lex::match_identifier(args:get(1)))
return 0